first = "Tobi"
last = "Omotayo"

# full = first + " " + last - using concatenation

# full = f"{first} {last}"

# full = f"{len(first)} {last}"

# put in any valid expression inside curly braces
full = f"{len(first)} {2 + 3}"

print(full)
